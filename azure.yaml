name: samplestore-search-agent
services:
    app:
        project: app
        host: containerapp
        language: csharp
        docker:
            dockerfile: Dockerfile    
            remoteBuild: true        